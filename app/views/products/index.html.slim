.page.page_common.page_products_all
  .page_products_all_list
    .products
      .products_new
        = link_to 'Добавить', [:new, :product]
      - @products.each do |product|
        .products_item
          strong
            = link_to product do
              | ##{product.id}
              =<> product.title_safe

          span.actions
            | (
            => link_to 'редактировать', [:edit, product]
            | /
            =< link_to 'цвета и размеры', [:variants, product]
            | )
