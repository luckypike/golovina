.form
  = simple_form_for(@collection) do |f|
    = f.error_notification

    .form_inputs
      = f.input :title
      = f.input :slug
      = f.input :desc
      = f.input :text
      = f.input :weight

    .form_actions
      = f.button :submit
